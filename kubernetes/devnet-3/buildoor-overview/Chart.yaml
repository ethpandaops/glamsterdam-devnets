# Manually staged. Replace with ansible-generated output once the chart
# (buildoor-overview 0.0.1) is published to the upstream helm-charts repo.

apiVersion: v2
name: buildoor-overview
description: A Helm chart for Kubernetes
type: application
version: 0.1.0
dependencies:
  - name: buildoor-overview
    repository: https://ethpandaops.github.io/ethereum-helm-charts
    version: 0.0.1
